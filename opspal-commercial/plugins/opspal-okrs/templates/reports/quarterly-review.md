# Quarterly OKR Review — <%= cycle %>

**Organization:** <%= org %>
**Quarter:** <%= quarter %>
**Generated:** <%= formatDate(date, 'long') %>

---

## Cycle Summary

| Metric | Value |
|--------|-------|
| **Total Objectives** | <%= totalObjectives %> |
| **Total Key Results** | <%= totalKRs %> |
| **KRs Hit** | <%= krsHit %> (<%= krsHitPct %>%) |
| **KRs Partial** | <%= krsPartial %> (<%= krsPartialPct %>%) |
| **KRs Missed** | <%= krsMissed %> (<%= krsMissedPct %>%) |
| **Overall Score** | <%= overallScore %>% |

---

## Key Result Outcomes

| Objective | Key Result | Target | Actual | Variance | Classification |
|-----------|-----------|--------|--------|----------|----------------|
<% outcomes.forEach(function(o) { %>
| <%= o.objective %> | <%= o.kr %> | <%= o.target %> | <%= o.actual %> | <%= o.variance %> | <%= o.classification %> |
<% }); %>

---

## What Worked

<% whatWorked.forEach(function(item) { %>
- **<%= item.title %>**: <%= item.description %>
<% }); %>

---

## What Didn't Work

<% whatDidntWork.forEach(function(item) { %>
- **<%= item.title %>**: <%= item.description %>
<% }); %>

---

## Calibration Summary

<% if (calibration) { %>
| Metric | Prior Bias | Adjusted Bias | Confidence |
|--------|-----------|---------------|------------|
<% calibration.forEach(function(cal) { %>
| <%= cal.metric %> | <%= cal.priorBias %> | <%= cal.adjustedBias %> | <%= cal.confidence %> |
<% }); %>

<% if (calibrationWarning) { %>
> **Note:** <%= calibrationWarning %>
<% } %>
<% } else { %>
Calibration data not yet available. Minimum 4 completed cycles required.
<% } %>

---

## Planning Implications for Next Cycle

<% planningImplications.forEach(function(impl) { %>
- **<%= impl.area %>**: <%= impl.recommendation %>
<% }); %>

---

*Generated by opspal-okrs v3.0.0 — OKR Learning Engine*
