# Monthly OKR Scorecard — <%= cycle %>

**Organization:** <%= org %>
**Month:** <%= month %>
**Generated:** <%= formatDate(date, 'long') %>

---

## Objective Summary

| Objective | Owner | Status | KR Completion | Trend |
|-----------|-------|--------|---------------|-------|
<% objectives.forEach(function(obj) { %>
| <%= obj.name %> | <%= obj.owner %> | <%= obj.status %> | <%= obj.completion %>% | <%= obj.trend %> |
<% }); %>

---

## Biggest Movement This Month

<%= biggestMovement.description %>

**Impact:** <%= biggestMovement.impact %>
**Driver:** <%= biggestMovement.driver %>

---

## Top 3 Risks

<% risks.forEach(function(risk, index) { %>
<%= index + 1 %>. **<%= risk.title %>** — <%= risk.description %> *(Owner: <%= risk.owner %>)*
<% }); %>

---

## Decisions Needed

<% if (decisions && decisions.length > 0) { %>
<% decisions.forEach(function(decision) { %>
- **<%= decision.title %>**: <%= decision.description %> *(Deadline: <%= decision.deadline %>)*
<% }); %>
<% } else { %>
No pending decisions this month.
<% } %>

---

*Generated by opspal-okrs v3.0.0 — OKR Cadence Manager*
