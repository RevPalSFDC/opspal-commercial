# Overflow Handling

On overflow:
- in `warn` mode, continue with truncated preview and clear warning;
- in `block` mode, stop and return remediation guidance.

Never silently truncate without notice.
