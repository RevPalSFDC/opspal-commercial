# Alias Linting Patterns

Primary source: `hooks/pre-write-alias-linter.sh`.

## Patterns

- Detect literal `--target-org` aliases in generated scripts.
- Prefer env vars (`SF_TARGET_ORG`) and resolver helpers.
