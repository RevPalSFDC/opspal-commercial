# Session Start Bootstrap

Primary source: `hooks/session-start-agent-reminder.sh`.

## Tasks

- Initialize temp context files and directories.
- Auto-detect and export target org where possible.
- Keep startup behavior non-blocking.
