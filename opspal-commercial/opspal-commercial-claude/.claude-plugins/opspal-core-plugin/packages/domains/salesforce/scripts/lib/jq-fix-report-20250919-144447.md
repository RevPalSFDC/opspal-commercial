# JQ Null Pattern Fix Report
Generated: Fri Sep 19 02:44:47 PM EDT 2025

## Vulnerable Patterns Found

### Direct Array Access (.result.records[])
${PROJECT_ROOT}  | jq '[.result.records[].OwnerId] | unique | {activeOwners: length}'
${PROJECT_ROOT}  | jq '[.result.records[].LeadId] | unique | {leadsWithCampaign: length}'
${PROJECT_ROOT}  | jq '[.result.records[].AccountId] | unique | {accountsWithOpps: length}'
${PROJECT_ROOT}  | jq '[.result.records[].AccountId] | group_by(.) | map(length) | add / length as $avg | {avgContactsPerAccount: ($avg // 0)}'
${PROJECT_ROOT} data query --query "SELECT Id FROM Report WHERE LastModifiedDate >= LAST_N_MONTHS:3 ORDER BY LastModifiedDate DESC LIMIT 10" --target-org "$ALIAS" --result-format json | jq -r '.result.records[].Id' | tr '\n' ' ')
${PROJECT_ROOT} -r '.result.records[] | "• \(.Name) (\(.DeveloperName)) - ID: \(.Id)"' record-types-raw.json
${PROJECT_ROOT} -r '.result.records[] | select(.DeveloperName == "Portal_Onboarding" or .Name | contains("Portal"))' record-types-raw.json)
${PROJECT_ROOT} -r '.result.records[] | select(.DeveloperName == "BRV" or .Name | contains("Blue Rabbit"))' record-types-raw.json)
${PROJECT_ROOT} ALL_RT_IDS="$(jq -r '.result.records[].Id' record-types-raw.json | tr '\n' ',' | sed 's/,$//')"
${PROJECT_ROOT} RT_ID in $(jq -r '.result.records[].Id' record-types-raw.json); do

### Pipe Operations (| .result.records)
${PROJECT_ROOT}    echo "### Pipe Operations (| .result.records)" >> "$report_file"
${PROJECT_ROOT}  | .result.records        → | (.result.records // [])

### Length Operations (.result.records | length)
${PROJECT_ROOT}    if echo "$QUERY_RESULT" | jq -r '.result.records | length' | grep -q "0"; then
${PROJECT_ROOT}FOLDER_COUNT=$(echo "$FOLDER_QUERY" | jq -r '.result.records | length')
${PROJECT_ROOT}REPORT_COUNT=$(echo "$REPORT_QUERY" | jq -r '.result.records | length')
${PROJECT_ROOT} data query --query "SELECT Contract__c, COUNT(Id) cnt FROM Subscription__c WHERE Contract__c != null GROUP BY Contract__c HAVING COUNT(Id) > 1" --target-org "$ORG_ALIAS" --json | jq '.result.records | length')
${PROJECT_ROOT} data query --query "SELECT Contract__c FROM Subscription__c WHERE Contract__c != null GROUP BY Contract__c HAVING COUNT(Id) > 1" --target-org "$ORG_ALIAS" --json | jq '.result.records | length')
${PROJECT_ROOT} "$FOLDER_QUERY" | jq -r '.result.records | length')
${PROJECT_ROOT} "$REPORT1_QUERY" | jq -r '.result.records | length')
${PROJECT_ROOT} "$REPORT2_QUERY" | jq -r '.result.records | length')
${PROJECT_ROOT} "$FOLDER_QUERY" | jq -r '.result.records | length')
${PROJECT_ROOT} "$ALL_REPORTS_QUERY" | jq -r '.result.records | length')
