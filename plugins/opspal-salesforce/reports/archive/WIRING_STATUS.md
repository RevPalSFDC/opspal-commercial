# Command Interception Wiring Status (Archive)

**Status**: Archived reference

Legacy CLI command interception has been superseded by sf-only enforcement.
All current workflows require the Salesforce CLI (`sf`). Legacy commands are blocked at runtime.

For current behavior and guidance, see:
- `scripts/lib/sf-command-interceptor.js`
- `docs/SF_CLI_COMMAND_MAPPING.md`
