# Budget Thresholds

Set and enforce explicit byte/token budgets per hook phase:
- context assembly budget,
- injected payload budget,
- final message budget.

Use stable clipping order and preserve critical compliance markers.
