# Pretask Context Loading

Primary source: `hooks/pre-task-context-loader.sh`.

## Requirements

- Produce machine-readable org context output.
- Include detection confidence and source.
- Support strict and non-strict modes.
