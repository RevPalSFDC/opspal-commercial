# Thresholds and Allowlists

Define:
- hard max bytes for staged files,
- warn threshold below hard block,
- extension/path allowlist for expected binaries or fixtures.

Document exceptions and keep allowlists narrow.
