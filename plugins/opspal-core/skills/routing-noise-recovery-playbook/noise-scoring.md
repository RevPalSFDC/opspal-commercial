# Noise Scoring

Model routing noise from transcript artifacts and repetitive scaffolding.
Use thresholded signals to downgrade low-value context and preserve user intent lines.

Reference: `plugins/opspal-core/hooks/unified-router.sh` computes transcript noise and applies confidence thresholds.
