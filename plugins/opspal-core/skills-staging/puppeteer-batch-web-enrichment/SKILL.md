---
name: puppeteer-batch-web-enrichment
description: "Headless Chrome with realistic UA, concurrent tab pool, browser crash recovery, multi-method extraction (JSON-LD, og:meta, selectors, regex) with subpage fallback"
version: 1.0.0
allowed-tools:
  - Read
  - Grep
  - Glob
agent: opspal-core:direct execution
---

# Puppeteer Batch Web Enrichment

Headless Chrome with realistic UA, concurrent tab pool, browser crash recovery, multi-method extraction (JSON-LD, og:meta, selectors, regex) with subpage fallback

## When to Use This Skill

- When the task matches the pattern described in the description

**Category**: automation
**Discovered from**: reflection analysis

## Workflow

1. Verify prerequisites — confirm the target org/object/context is appropriate
2. Execute the pattern: Headless Chrome with realistic UA, concurrent tab pool, browser crash recovery, multi-method extraction (JSON-LD, og:meta, selectors, regex) with subpage fallback
3. Validate the outcome — confirm the expected result was achieved

## Source

- **Reflection**: 011c05a4-a5cf-473f-b13b-62508bde610a
- **Agent**: direct execution
- **Enriched**: 2026-04-03
