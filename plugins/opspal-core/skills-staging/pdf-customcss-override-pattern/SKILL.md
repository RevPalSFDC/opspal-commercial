---
name: pdf-customcss-override-pattern
description: "When default CSS components conflict (e.g., double page breaks), use the customCSS option as the final CSS layer to inject !important overrides without modifying plugin files."
version: 1.0.0
allowed-tools:
  - Read
  - Grep
  - Glob
---

# Pdf Customcss Override Pattern

When default CSS components conflict (e.g., double page breaks), use the customCSS option as the final CSS layer to inject !important overrides without modifying plugin files.

## When to Use This Skill

- During data import or bulk operations

**Category**: configuration
**Discovered from**: reflection analysis

## Workflow

1. When default CSS components conflict (e
2. , double page breaks), use the customCSS option as the final CSS layer to inject !important overrides without modifying plugin files

## Source

- **Reflection**: 5afd35cd-d5ec-4733-b369-839edfe01f10
- **Agent**: manual (direct PDFGenerator invocation)
- **Enriched**: 2026-04-03
