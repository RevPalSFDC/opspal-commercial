# Weekly KR Progress Update — <%= cycle %>

**Organization:** <%= org %>
**Week Ending:** <%= formatDate(date, 'long') %>
**Prepared By:** OKR Cadence Manager

---

## Five-Number Scoreline

| Metric | Value |
|--------|-------|
| **Objectives On Track** | <%= onTrackCount %> / <%= totalObjectives %> |
| **KRs Green** | <%= greenKRs %> |
| **KRs Yellow** | <%= yellowKRs %> |
| **KRs Red** | <%= redKRs %> |
| **Overall Completion** | <%= overallCompletion %>% |

---

<% objectives.forEach(function(obj) { %>
## <%= obj.name %>

**Owner:** <%= obj.owner %> | **Status:** <%= obj.status %> | **Health Delta:** <%= obj.healthDelta %>

| Key Result | Current | Target | Progress | Status |
|-----------|---------|--------|----------|--------|
<% obj.keyResults.forEach(function(kr) { %>
| <%= kr.name %> | <%= kr.current %> | <%= kr.target %> | <%= kr.progress %>% | <%= kr.status %> |
<% }); %>

<% if (obj.interventions && obj.interventions.length > 0) { %>
**Interventions Needed:**
<% obj.interventions.forEach(function(item) { %>
- <%= item %>
<% }); %>
<% } %>

<% }); %>

---

## Data Gaps

<% if (dataGaps && dataGaps.length > 0) { %>
The following KRs have stale or missing baseline data:
<% dataGaps.forEach(function(gap) { %>
- **<%= gap.kr %>**: <%= gap.reason %>
<% }); %>
<% } else { %>
No data gaps detected this week.
<% } %>

---

*Generated by opspal-okrs v3.0.0 — OKR Cadence Manager*
